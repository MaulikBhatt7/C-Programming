#include<stdio.h>

int factorial(int n);
void main()
{
	int n;
	
	printf("Enter n=");
	scanf("%d",&n);
	
	printf("Answer=%d",factorial(n));
	
	
}

int factorial(int n)
{
	if(n==1)
		return 1;
		
		return factorial(n-1)*n;
}
